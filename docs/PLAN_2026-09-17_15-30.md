# PLAN — Audit UI/UX A11y + Hallmark Anti-Pattern

**Tanggal:** 2026-09-17
**Branch:** `main`
**Status:** ✅ Selesai (2 commit)

---

## Fase 1: Impeccable — Deteksi Kontras Aksesibilitas

**Metode:** `detect --json src` → verifikasi tiap temuan terhadap markup + hitung rasio kontras manual.

**False positive (11×):**
- `gray-on-color` — shade 500-OK pada chip `*-100`, kontras 3.3–4.2:1
- `bounce-easing` — animasi terkontrol, bukan ease-in-out generik
- `layout-transition` — grid/list toggle dengan `transition-all` disengaja untuk mode toggle
- `cramped-padding` — padding konsisten dengan sistem
- `side-tab` — bukan tab horizontal, elemen menu vertikal

**Diterapkan (6 perbaikan):**

| Elemen | File | Perubahan | Rasio Sebelum | Rasio Sesudah |
|---|---|---|---|---|
| Ikon toast (amber) | `ToastContainer.tsx` | `amber-500` → `amber-700` | 2.1:1 | 4.9:1 |
| Ikon toast (primary) | `ToastContainer.tsx` | `primary-500` → `primary-700` | 3.6:1 | 6.5:1 |
| Ikon toast (red) | `ToastContainer.tsx` | `red-500` → `red-700` | 3.1:1 | 5.8:1 |
| Ikon StatMini amber | `EventAreaManagerModal.tsx` | `amber-500` → `amber-700` | 2.8:1 | 5.2:1 |
| Teks SearchBar | `SearchBar.tsx` | `slate-500` → `slate-600` | 3.4:1 | 4.7:1 |
| `.landing-grid` mati | `motion.css` | Hapus kelas tidak terpakai | — | — |

**Hasil:** Commit `babe937`.

---

## Fase 2: Hallmark — Audit Anti-Pattern AI-Generated Design

**Metode:** Muat katalog anti-pattern + `DESIGN.md` → periksa permukaan flagship (`/`, `/events`, `/pameran`, `/sponsor`, gallery, community).

**Temuan (8):**

| # | Severity | Temuan | Keputusan |
|---|---|---|---|
| 1 | Critical | Gradient CTA — `bg-gradient-to-br` tosca | **Pass** — radial bloom tipis, bukan 3-stop loud gradient |
| 2 | Major | Stamp `tokens.css` — `custom (Graphify-tosca)` | **Fixed** — ganti ke `Metmal tosca/pink warm paper` + tambah `design-system: DESIGN.md (root)` |
| 3 | Major | Eyebrow → section title jarak dekat | **Pass** — semua vertical-stack, bukan 2-kolom yang di-ban |
| 4 | Major | Confirm-toggle destruktif | **Dipertahankan** — keputusan proyek tertutup (13 situs `confirm({})`) |
| 5 | Major | `hover:scale-105` pada card galeri | **Pass** — konsistensi galeri yang disengaja |
| 6 | Major | Icon-tile tidak seragam | **Pass** — ikon multi-warna adalah kunci visual per field |
| 7 | Major | Bare `transition` — 8 lokasi publik | **Fixed** |
| 8 | Minor | Shadow elevation tidak bertahap | **Pass** — variasi disengaja per komponen |

**Diterapkan — bare `transition` (8 lokasi):**

| File | Elemen | `transition` → |
|---|---|---|
| `FeaturedEvents.tsx` | Kartu event | `transition-shadow transition-transform` |
| `CommunityEventAreas.tsx` | Kartu area | `transition-shadow` |
| `CommunityBenefits.tsx` | Tombol CTA | `transition-colors` |
| `CommunityUpcomingEvents.tsx` | Tombol CTA header | `transition-colors` |
| `CommunityUpcomingEvents.tsx` | Kartu main event | `transition-shadow` |
| `CommunityUpcomingEvents.tsx` | Tombol CTA main | `transition-colors` |
| `CommunityUpcomingEvents.tsx` | Kartu sub-event | `transition-shadow` |
| `CommunityUpcomingEvents.tsx` | Link "Lihat Semua" | `transition-colors` |

**Hasil:** Commit `50317ba`.

---

## Pending / Belum Dikerjakan

- [ ] Full `NODE_ENV=test npx vitest run` — targeted tests (SearchBar + UserManagement + useToast) hijau; full suite belum.
- [ ] `PRODUCT.md` — masih ada `## Register` deprecated, belum ada schema-stamp v4. Perlu `$impeccable init`, menunggu persetujuan user.
- [ ] ~71 bare `transition` di luar permukaan publik (admin modal, internal components) tidak disentuh — scope audit dibatasi ke publik.

## Tidak Disentuh

- Folder `presentasi/` (WIP deck user)

## Decision Record

- **Prefer shade 700** untuk ikon di atas tint background karena tint `opacity-40` menurunkan kontras efektif.
- **Multi-warna ikon dipertahankan** — cyan, emerald, amber adalah kunci visual per tipe field di `EventDetailContent`.
- **Confirm-toggle TIDAK diubah** — pola `confirm({})` adalah keputusan proyek tertutup (ADR + AGENTS.md).
- **Bare `transition` hanya publik** — 71+ internal tidak disentuh, scope terbatas + diff minimal.